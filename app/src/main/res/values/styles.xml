<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
    <item name="tabIndicatorColor">@color/golden</item>
    <item name="tabIndicatorHeight">4dp</item>
    <item name="tabPaddingStart">4dp</item>
    <item name="tabPaddingEnd">4dp</item>
    <item name="tabTextAppearance">@style/MineCustomTabText</item>
    <item name="tabSelectedTextColor">@color/golden</item>
    </style>

    <style name="MineCustomTabText" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">@dimen/_14ssp</item>
        <item name="android:indicatorStart">4dp</item>
        <item name="android:indicatorEnd">-4dp</item>
    </style>

</resources>
